import React from "react";

export default ({ year }) => {
  return (
    <div className="preview">
      <div>{year}</div>
      <div>{year}</div>
      <div>{year}</div>
      <div>{year}</div>
      <div>{year}</div>
      <div>{year}</div>
    </div>
  );
};
